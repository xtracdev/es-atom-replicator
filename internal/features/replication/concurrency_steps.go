package replication

import . "github.com/gucumber/gucumber"

func init() {
	Given(`^two replicators$`, func() {
		T.Skip() // pending
	})

	When(`^both are started$`, func() {
		T.Skip() // pending
	})

	Then(`^only one may execute the catch up logic$`, func() {
		T.Skip() // pending
	})

	When(`^start up is complete and steady state is reached$`, func() {
		T.Skip() // pending
	})

	And(`^there are recent events$`, func() {
		T.Skip() // pending
	})

	Then(`^only one replicator processes the recent events$`, func() {
		T.Skip() // pending
	})

}
